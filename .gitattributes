# cmd.exe expects CRLF in .bat files. GitHub's ZIP (git archive) honours this; the LF blob served by
# raw.githubusercontent.com is only ever used for doctor.sh / doctor.ps1, which do not care.
*.bat text eol=crlf
